# Seeded from MMEXOFAST solution 0 (0-indexed)
# n_solutions in file: 2
#
# MMEXOFAST uncertainties are mapped to init_scale, NOT sigma.
# init_scale = sampling hint only (no logp penalty).
# sigma = Gaussian prior (would double-count the data).

source.Source.t_0:
    initval: 2458199.234148761025
    init_scale: 0.0021618658356601372

source.Source.u_0:
    initval: 0.06893061672414033
    init_scale: 0.0016174163103184272

# t_E is derived in EXOZIPPy from stellar masses/distances/proper motions.
# Provided here as an initval hint to seed the relaxation engine.
mulensevent.t_E:
    initval: 11.56142700504093
    init_scale: 0.2129798337082045

lens.Companion.s:
    initval: 2.420300352396205
    init_scale: 0.0135098224

# alpha: relaxation engine propagates initval/init_scale to cosalpha/sinalpha.
lens.Companion.alpha:
    initval: 1.8322968914160362
    init_scale: 0.048835429805909714

source.Source.rho:
    initval: 0.024008570843962032
    init_scale: 0.0883007029

# q = M_companion / M_primary.  EXOZIPPy's relaxation engine propagates
# this through the symbolic relation  q * M_primary = M_companion
# to set the companion's mass initval automatically.
lens.Companion.q:
    initval: 0.0002225978981030849
    init_scale: 0.0000138432

star.Lens.ra:
    initval: 268.68075
    sigma: 0
star.Lens.dec:
    initval: -28.739278
    sigma: 0
star.Source.ra:
    initval: 268.68075
    sigma: 0
star.Source.dec:
    initval: -28.739278
    sigma: 0

# The lens star's radius/teff/feh and both stars' teff/feh used to be pinned
# by hand here ("these shouldn't be fit for such models ... temporary hack").
# They are deactivated automatically now (review 3.8.1): with no SED and no
# evolutionary model nothing reads them, so a hand pin was DROPPED with a
# warning on every build.  The pins are gone; add an `sed:` block and the
# six become free, constrained parameters again with no edit here.
planet.Companion.radius:
    sigma: 0

# The SOURCE radius is different: finite_source is on, so rho =
# theta_star/theta_E DOES read it, and this pin is live.  It is held fixed
# because nothing here (no SED) constrains it independently of theta_E.
star.Source.radius:
    sigma: 0.0

band.I.u1:
    initval: 0.5
    sigma: 0.2