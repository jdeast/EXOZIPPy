# ConfigManager, the relaxation engine, and parameter naming

Everything that resolves a number before any PyMC node exists: `src/exozippy/config.py`
(defaults, hints, overrides, the relaxation engine, the three user-facing spellings of an
element) and `src/exozippy/linking.py` (link expressions in a params file).

Read this before adding a hint or override channel, changing how a config value is
resolved, or touching the parameter naming or link syntax. Related: parameters themselves
are `src/exozippy/components/parameter.md`; the manifest vocabulary is
`src/exozippy/components/components.md`. The ban on writing into
`config_manager.user_params` from a component is one of the invariants kept in full in the
trunk `CLAUDE.md`.

## ConfigManager and the relaxation engine

`src/exozippy/config.py` is the initialization backbone. It:

1. Loads all `defaults.yaml` files (one per component subdirectory) into `base_defaults`.
2. Loads all `symbolic_physics.py` files; calls `get_symbol_map()` to translate abstract symbols to indexed paths (`star.0.mass`, `lens.0.t_E`, …) and collects `RELATIONS` (SymPy equations).
3. Manages a **provenance ranking** system. Higher rank wins when values conflict:
   - `RANK_USER = 100` — from `params.yaml`
   - `RANK_DERIVED_DATA = 60` — from data (e.g., RV offset from median)
   - `RANK_DERIVED_MIXED = 40` — solved using a mix of user + defaults
   - `RANK_DEFAULT = 20` — from `defaults.yaml`
   - rank 30 is used for microlensing distance hints (overrides 10 pc default, yields to user)
4. Runs a **relaxation engine** (`resolve_and_validate_parameters`): iteratively substitutes known values into the SymPy equations to derive unknowns and detect contradictions.
5. Collects `init_scale` values from defaults, component hints, user sigmas, and the engine's own solved-value sync. These are **preliminary only** (see "Whitening", now `src/exozippy/whitening.md`): sampled parameters get their real scale from the startup probe, and soft-bound barrier steepness is measured numerically post-whitening (the old sympy forward/backward Jacobian scale passes are deleted). `init_scale` is **not user-facing**: entries in a params file are stripped with a warning at ConfigManager construction.

Components push hints via `config_manager.add_hint(path, value, rank)` during stage 1–2. The hint system is the correct way for components to propose data-driven initial guesses; they are layered in after defaults but before the solver runs. It is the **only** correct way for a start value: a plain manifest `"initval"` option is merged as `{**cfg, **options}` *after* `resolve()`, so it never acquires a rank and beats the user's params file outright. `transit.baseline` (the per-file median flux) carried its median that way until 2026-08 and so silently discarded an explicit `transit.<name>.baseline` -- including the EXOFASTv2 solution shipped in `examples/gj1214`, whose start logp is the one shipped example the move to `Transit._hint_baseline` changes. The measurement happens in `load_data` (stage 1a) and the hint is pushed in `register_parameters` (stage 2), which is the whole reason the channel is available at all; a non-finite median is skipped with a warning so the defaults.yaml 1.0 (also `load_data`'s own no-data seed) stands. Compare `rvinstrument`'s `gamma` (median RV), `mulensinstrument`'s `f_source`/`log_f_total`/`q_source`, `Instrument._prepare_gp`'s amplitude and `planet.K`, all of which were already hints.

Every hint channel -- `add_hint`, `add_scale_hint`, `add_seed_hints`, `seed_start_value` -- turns the component's path into the index form `resolve()` looks up and its number into internal units through the **one** helper `ConfigManager._translate_and_scale`, which in turn defers to `canonical_param_key` for the translation itself. Both halves matter and both fail silently: a hint filed under an untranslated `star.Lens.distance` is never found by `resolve()` (the component's bulge scale vanishes and the generic default stands), and a hint left in the user-facing unit is wrong by whatever the conversion factor is (57x for a deg -> rad angle). `add_scale_hint` carried a line-for-line copy of that body until 2026-08, under a docstring that already claimed the sharing. Cover: `tests/test_config_hint_paths.py`.

Manifest options that carry a **structural** value -- a shape, an `expr_key`, a `table_note`, a `force_node`, a `mask`, or a bound/start a component derives from the *config* to select one branch of a degeneracy (`Orbit._restrict_bigomega_halfplane`'s `xbigomega`/`ybigomega`/`secosw`/`sesinw`/`tc`, whose whole job is to override the engine's seed; `Orbit`'s `tc` window; `cosi`'s `lower`) -- are not start values and stay options.

Manifest entries take an `"overrides"` dict (`{field: value_or_per_element_list}`, forwarded to `resolve(internal_overrides=...)`) for component-computed per-element **defaults**. Unlike the manifest's other options, which are merged *over* the resolved config and so beat the user's params file, these are layered *under* it — use them whenever the user must still be able to override. `NaN` in a per-element list means "leave this element alone"; `±inf` is a real bound and is applied. `apply_value` special-cases the two bound keys as `max(lower)` / `min(upper)`, and the user's own params go through that same function, so a bound supplied through `"overrides"` combines as `max(user_lower, component_floor)` **order-independently**. That is what makes `"overrides"` the right channel for a component-imposed floor (see the IMF floor in `src/exozippy/components/star/star.md`, and `Instrument._register_noise`'s jitter-variance floor) and `"options"` the wrong one: options would replace -- and so silently *lower* -- a bound the user had tightened. Because the combination is a clip, a component bound may still beat the user's; reserve bound overrides for **validity** limits (past which the likelihood is NaN or infinite), not for preferences, and note that `resolve` logs a warning naming the parameter whenever an override bound clips a user's.

`ConfigManager.add_override(path, **fields)` is the same `"overrides"` channel reached **by path**, for a component that must constrain a parameter another component owns and so has no manifest to hang it on. It takes any of the three spellings `resolve()` accepts (`comp.param` broadcasts to every element), values are in the parameter's defaults.yaml unit, and `resolve()` applies it through the same `apply_value` pass as `internal_overrides`, before the user's params -- so all of the paragraph above holds verbatim, warning included. Its one caller today is `SED._inject_grid_bounds`, whose BC grid coverage bounds `star.teffsed`/`star.loggsed`/`star.feh`/`star.av` -- hard bounds on the three sampled ones, a soft barrier on the derived `loggsed` (see "Bands, filters, and SED flux constraints", now `src/exozippy/components/sed/sed.md`).


`src/exozippy/components/defaults.yaml` (no component key, straight parameter names) holds root-level defaults shared by several components; `resolve()` layers the component's own block over it. Today it carries only the GP blueprint.

## Parameter naming convention

An element has **three** user-facing spellings, and `ConfigManager.resolve()` accepts all three: the 2-part **broadcast** `comp.param`, the **index** form `comp.<i>.param`, and the **name** form `comp.<Name>.param` (e.g. `star.Lens.distance`). Internally, instance names are standardized to indices (`star.0.distance`).

**The most specific spelling wins, for every field.** The two specific spellings each name one element and beat the broadcast, which covers only the elements no specific entry claims -- and that holds for the `unit:` and `init_scale` channels exactly as it holds for `initval`/bounds/`mu`/`sigma`. `resolve()` builds the candidate list once, in `_element_keys` (least specific first), and its five consumers split into two traversals of that ONE list: the loops that apply every match in turn (component `"overrides"`, user params) let the most specific land last, and the lookups that stop at the first hit (`unit:`, `propagated_scales`, `scale_hints`) go through `_lookup_keys`, which rotates the broadcast key to the end. Until 2026-08 the first-hit lookups scanned the list as written, so one config could have `star.teff: {unit: K}` **beat** `star.0.teff: {unit: ...}` for the unit and for an `init_scale` while **losing** to it for every numeric field. Not cosmetic on the scale channels: `init_scale` seeds the whitening probe, and for an unbounded element with no sigma it *is* the prior width.

**Naming one element under BOTH specific spellings is an error and raises.** The index and name forms are equally specific, so "most specific wins" cannot adjudicate, and the two traversals disagreed about it (index won the first-hit lookups, name won the apply-every-match loops). Rather than pick a winner, `_reject_duplicate_spellings` refuses the config, naming both keys. It runs at `ConfigManager` construction on the **raw** params -- before `standardize_param_names`, which is where the evidence is destroyed: pass 1 files both under the one index key, so whichever the YAML listed second silently overwrote the other, value, bounds and **prior** included. Content is not consulted; two entries that happen to agree are still one element addressed twice. `resolve()` carries the residual half of the same check, for the case construction cannot see: per-element `names` supplied by a component's manifest that are not its own config instances' names (`lens`'s per-source vectors, labelled with the SOURCE STARS' names -- `examples/ob161003`), where both keys really do reach `resolve()` live. That check reads the keys the **user wrote**, not `user_params`, because `finalize_user_params` injects the engine's solution back under the index form and on ob161003 those coexist with the user's name-form entries by design.

**Broadcast + specific is NOT affected** and must not be: it is well defined ("most specific wins"), it is the idiom for "set every element, refine one", and shipped configs plus `SED._inject_grid_bounds`' `add_override` on `star.av`/`feh`/`teffsed` rely on it. Two consequences of the raise: the reported `unit` *string* (written by the user-params loop) can no longer disagree with the *scaling* (set by the first-hit scan), since at most one specific entry exists per element and both passes therefore read the same one; and a name that is also an index string is not a duplicate at all (the two spellings are then one string, and `validate_instance_names` bans all-digit names anyway). No shipped example moves -- all 19 configs prepare and resolve byte-identically, ledger and provenance included, and none names an element twice. Tests: `tests/test_param_specificity.py`.

## User-defined parameter links (`linking.py`)

Any of the five linkable numeric fields in a `params.yaml` entry (`initval`, `mu`, `sigma`, `lower`, `upper` -- `linking.LINKABLE_FIELDS`) may be a string expression referencing other parameters (`star.A.age: {initval: star.B.age, sigma: 0}`, `orbit.b.omega: {initval: "orbit.c.omega + 180", sigma: 0}`, `star.A.av: {lower: star.B.av}`). Semantics:
- `initval` link + `sigma: 0` → hard link: the element is never sampled and deterministically tracks the expression.
- `initval` link + `sigma > 0` (or a `mu` link) → soft link: sampled normally plus a Gaussian `pm.Potential` on the difference.
- `initval` link, no sigma → initialization seeding only (relaxation-engine snapshot, no runtime tie).
- `lower`/`upper` link → dynamic hard bound: the logit transform maps into the tensor-valued interval. **No `-log(span)` normalization potential** -- the reparameterization already supplies it. With `lq = c + s*raw` the raw-space density is `q(1-q)` (section C cancels the raw `N(0,1)`) and `dval/draw = span*q*(1-q)*s`, so `p(val) = 1/(sqrt(2pi)*s*span)`: the conditional is exactly `U(lower, upper)` and its integral is independent of `span`, for any span. An explicit `-log(span)` would double-count, multiplying the joint by another `1/span` and rewarding the *bound source* for shrinking the interval (an ordering link `star.A.av: {lower: star.B.av}` with `av` in `[0, 100]` gave `av_B` a spurious `1/(100 - av_B)` factor pushing it to the wall; tight spans bias hard). This is review note 1.5; a term that was there until 2026-08-10. Do not "restore the normalization".
- `sigma` link → static numeric snapshot from the relaxation-engine solution. (`init_scale` is not linkable — it is stripped from user params.)

Referenced parameters contribute their values in **their own user units**; the result is read in the **target's user unit**. `ConfigManager` extracts links at construction (`extract_links` strips the strings from `user_params`), the relaxation engine asserts `initval`/`mu` links as directed RANK_USER assignments each iteration, `Component._wire_user_links` builds the PyTensor closures (same-parameter element references are resolved inside `build_pymc` via `set_subtensor`; cross-parameter references use the lazy `add_parameter` recursion), and `graph.py` adds cross-parameter build-order edges. Tests: `tests/test_linked_params.py`.

