* switching planet.mass to earthMass is converted back to jupiter in the diagnostic table. fix that.

* write unit tests for high coverage

* make CelestialBody super class of Star/Planet

* refactor system topology so planet owns it own host star and orbit rather than passing maps around. not sure how to do that.

* more features/components:
  - microlensing
  - transits
    - ttvs
    - long cadence
    - light travel time
  - GPs
    - both RVs and Transits, periodic or red
  - SED
    - home grown
    - MIST BC table
  - evolutionary models
  - astrometry
  - nbody
  - doppler tomography

* vcve parameterization:
 - refactored to remove the sign parameter. that means computing both solutions and incorporating the joint logaddexp summed likelihood, with soft and hard shields that allow us to compute a single log likelihood when given an imaginary/non-physical e. The soft shield (sigmoid) guides the sampler and protects the physics. the hard shield (clip) is a stop to protect the math.
 - cosw/sinw must be gaussian normals mu=0, sigma=1 (no bias to omega, no weird boundaries).

* analytic treatment of linear parameters (detrending, f0, gamma, probably mulensing baseline)

* custom circular model logic, possibly including analytic planet.K -> planet.mass solver

* add user input -> model parameter transformation layer (analogous to step2pars.pro)

* user linkable parameters (with symbolic math?)

blind spots:
  * checkpoints
  * pytensor cache bloat
  * large n compilation choke (?? is that AI trash?)
